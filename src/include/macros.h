//
// Created by y123456 on 2021/9/20.
//

#ifndef TINY_C_MACROS_H
#define TINY_C_MACROS_H

#define MAX(a,b) \
    (a) > (b) ? (a) : (b)

#define MIN(a,b) \
    (a) < (b) ? (a) : (b)


#endif //TINY_C_MACROS_H
